name: labeler

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  label:
    runs-on: ${{ vars.CI_RUNNER || 'homelab-runners' }}
    steps:
      - uses: actions/labeler@v5
